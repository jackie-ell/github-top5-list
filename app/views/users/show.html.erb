<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Show</title>
  </head>
  <body>
    <h1><%= @user %></h1>

    <h3>Top 5 repositories:</h3>
    <% @user_repos.each do |repo| %>
      <% if repo != nil %>
        <a href="<%= repo["html_url"] %>"><%= repo["name"] %></a>
        <br>
        <br>
      <% end %>
    <% end %>
  </body>
</html>
